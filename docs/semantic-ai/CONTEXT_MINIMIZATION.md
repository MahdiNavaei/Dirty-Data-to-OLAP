# Context Minimization

The semantic boundary consumes only project-owned aggregate contracts and one subject or relationship at a time. Context manifests contain safe fields, evidence references, scope, counts, redaction policy and fingerprints; they do not contain staged rows, raw values, provider prompts, working tables or estimator objects. Subject, item, character, evidence-reference and provider-call limits are enforced before generation.
