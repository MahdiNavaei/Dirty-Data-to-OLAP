# Step18 Handoff

Step18 should evaluate the frozen Step17 policy using decision IDs, candidate
subjects, raw/normalized signal references, scope/reliability, contributions,
coverage, uncalibrated scores, bands, conflicts and missingness. Benchmark
truth labels remain separate from runtime fusion input.

Step17 handoff: `Step18 — ML Evaluation Engineer`. G5 remains `PENDING`; no
Step18 implementation is included here.
