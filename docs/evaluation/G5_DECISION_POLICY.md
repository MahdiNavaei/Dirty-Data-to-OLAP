# G5 Inference Validity Decision Policy

G5 may be formally `PASS` with a separate `inference_validity_mode=REVIEW_ONLY_VALIDATED` only when the frozen protocol is bound before TEST, groups are disjoint, every required provider output is normalized and loaded for the claimed task, truth is isolated from runtime inference, task metrics and denominators are present, artifact hashes verify, and calibration/threshold controls remain non-authorizing.

The historical Step18 v1 report claimed `REVIEW_ONLY_VALIDATED` from real provider receipts, but its headline metrics were not bound to those outputs. The final v4 closure supersedes that quality claim: Desbordante, Profiling, and Quality outputs are consumed by the evaluator; Valentine and Splink remain unavailable; relationship normalized reproducibility is explicitly `FAIL`; formal G5 is `PENDING`. Automation remains disabled and the assessment explicitly says `NOT_AUTHORIZED`. Optional semantic AI was not executed. This status is not production, release, acceptance, canonical identity, or repair approval.
