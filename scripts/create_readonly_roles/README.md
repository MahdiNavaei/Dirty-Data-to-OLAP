# Read-only role templates

This directory contains documentation-only guidance. It intentionally has no
real usernames, passwords, administrator credentials or executable grants.
Use provider-specific reviewed migrations, grant only the matrix in
`docs/security/LEAST_PRIVILEGE_ROLES.md`, revoke inherited/public privileges,
and verify effective grants before registering a source.
