# Step18 Inference Baseline

Status: `PASS`

Current baseline: `step18-inference-baseline-v4`; protocol: `step18-evaluation-protocol-v4`; seed: `20260911`. The v1, v2, and v3 runs remain historical and are retained separately. Step19 was not started.

The v3 chain is shared provider fixture -> real adapter or explicit unavailable receipt -> normalized project-owned output -> immutable content-hashed receipt -> exact population binding -> isolated truth join -> TEST metric. Development is diagnostic-only; calibration is threshold/calibration-only.

The v4 chain adds persistent project-local optional-provider sandboxes, corrected singleton ER truth and split roles, actual candidate/Fusion metrics, explicit unavailable schema/ER semantics, and a reproducibility negative control. The optional pinned installs failed during provisioning; no schema or ER result is represented as a metric.

TEST relationship candidate-generation recall is `5/7 = 0.7143`: 17 candidates, 5 true, 12 false, and missing positives `rq-orphan-10` and `rq-missing`. Fusion is `EVALUATED` from actual DependencyResult, ProfileResult, and QualityResult artifacts, with 17 decisions and no failures. Profile and Quality completed for all 13 relationship scenarios.

Provider status: Desbordante `EXECUTED`; Profiling `EXECUTED`; Quality `EXECUTED`; Valentine `INSUFFICIENT_EVIDENCE`; Splink `INSUFFICIENT_EVIDENCE`. Valentine `1.0.0` and Splink `4.0.17` were attempted in isolated project-local runtimes, but installation failed after the configured proxy was refused and no matching distribution was available. Schema and entity quality metrics are therefore not claimed, and no Coma/Cupid family aggregation or ER heuristic is substituted.

Bootstrap uses 7 TEST groups and 1,000 replicates; the relationship recall interval is `[0.4286, 1.0]`. Calibration sufficiency is computed and remains insufficient for a calibrated score claim. The threshold frontier is calibration-only, `STUDIED_ONLY`, with no selected threshold and no runtime-policy mutation. Automation is `NOT_AUTHORIZED`.

G4 and G4A remain PASS. Formal G5 remains `PENDING` with `inference_validity_mode=UNVALIDATED`. This is synthetic/aggregate-safe evidence only, not production, temporal, causal, human-acceptance, canonical-identity, repair, deployment, or release evidence.

The v4 continuation records semantic provider reproducibility `PASS` across two clean relationship executions while retaining unequal exact artifact-byte hashes. This is not a contradiction: the semantic fingerprint excludes only documented volatile envelope fields, while exact content hashes remain evidence. `REPRODUCIBILITY_PASS=true`; G5 remains pending because the required schema/entity provider outputs are unavailable. See `workspace/runs/step18-inference-baseline-v4/evaluation/reproducibility/relationship_provider.json`, `report.json`, and `g5/assessment.json`.

Provisioning records explicit package/version/dependency contracts and classifies both blocked installs as `NETWORK_TIMEOUT`. The normal optional-provider integration contract is clean-environment `33 passed, 2 skipped`; no sentinel runtime directories are required or created. The calibration report now records precise insufficiency reasons, and the calibration-only threshold frontier includes eligibility, selection, coverage, TP/FP, precision/recall, review remainder, and conflict/incomplete abstention. No threshold was selected or promoted.

## Current Offline Closure

The current v4 continuation used the complete local wheelhouse with `--no-index --find-links` and no network access. Valentine `1.0.0` and Splink `4.0.17` are both import/version verified in dedicated Python `3.10.11` runtimes. Valentine ran all 11 schema groups: Coma completed 11/11; Cupid completed 2/11 and failed 9/11 because the offline wheelhouse lacks the optional NLTK corpora. The receipt is immutable and records `INCOMPLETE`; no schema matcher ranking or schema Fusion quality metric is claimed. Real schema producer contracts completed for 22 source-side results: DataProfiler, QualityAnalysisService, and Desbordante DependencyDiscoveryService all report `COMPLETE`.

Splink processed the full 24-record `LINK_AND_DEDUPE` population, including `crm-r9/crm-r10`, but real EM training failed closed because at least one comparison level had no observed estimate and `_is_fully_trained` was false. No defaults, heuristic, labels, or fabricated ER output were used. ER metrics and the ER mutation control are therefore not available. G5 remains `PENDING` / `UNVALIDATED`; automation remains disabled; no threshold was selected; Step19 was not started.

## Final Offline Closure

The final v4 rerun used only the local wheelhouse and project-local NLTK data. NLTK preflight is `COMPLETE` for `punkt_tab`, `stopwords`, `wordnet`, and `omw-1.4`; the version, resource hashes, path, and disabled global search are bound in evaluation metadata. Valentine `1.0.0` completed Coma `11/11` and Cupid `11/11`.

Independent TEST matcher ranking is retained per family. Coma has no eligible multi-candidate query and therefore reports undefined ranking values with an explicit denominator; Cupid reports MRR `0.5000`, Recall@1/@3 `0.0000/1.0000`, and NDCG@1/@3 `0.0000/0.6309`. Joint candidate generation from the combined result is `3/3` recall, `3/5` precision, with `5` candidates and `0` no-match false positives.

Joint Schema Fusion uses one combined SchemaMatchResult per scenario and the real `22/22/22` Profile/Quality/Dependency producer estate. TEST Fusion is `EVALUATED`: `5` decisions, precision/recall/F1 `0.6000/1.0000/0.7500`, AP `0.4778`, MRR `0.5000`, Recall@1/@3 `0.0000/1.0000`, NDCG@3 `0.6309`, no failures, and review-only decisions. The mapping evidence exposes both Coma and Cupid rank dimensions.

Splink `4.0.17` is `COMPLETE` and trained with `cmp-name`, `cmp-email`, `cmp-phone` plus EM rules `block-email` and `block-phone`, over the full 24-record `LINK_AND_DEDUPE` population. ER TEST metrics cover 10 records and 45 TN pairs: TP `0`, FP `0`, FN `8`, recall/F1 `0.0000`, precision undefined for no strong links, false merges `0`, contaminated clusters `0`, false splits `0`, and `5` review edges. Mutation, truth-shuffle, input-order, population, leakage, and semantic reproducibility controls pass.

Final state is formal G5 `PASS`, `REVIEW_ONLY_VALIDATED`, automation `NOT_AUTHORIZED`, and selected threshold `NONE`. This remains synthetic aggregate-safe evidence and is not a production or temporal claim. Step19 implementation was not started.
